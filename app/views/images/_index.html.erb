<h1>Gallery</h1>
<div id="gallery">
	<% binding.pry %>
	<% @images.each do |image| %>
	<div class="image">
		<%= image.url %><br>
		<%= image.date %><br>
		<%= image.trail_name %><br>
		<%= image.username %><br>

	</div>
	<% end %>
	<%= link_to "Add Image", [:new, @imageable, :image] %>
</div>
