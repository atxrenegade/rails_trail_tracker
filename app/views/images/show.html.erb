<div class="image">
	<img src="<%= image.url %>"><br>
	<%= @image.date %><br>
	<%= @image.trail_name %><br>
	<%= @image.username %><br><br>

	<%= link_to "Add Image", [:new, @imageable, :image] %>
</div>
